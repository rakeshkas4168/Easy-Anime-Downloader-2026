#pragma once

class Downloader {
public:
    void Initialize() {
        // Setup download path
    }
    
    void StartDownload(const char* url) {
        // Handle download process
    }
};